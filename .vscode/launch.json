{
    "configurations": [
        {
            "name": "Debug",
            "type": "dotnet",
            "request": "launch",
            "projectPath": "${workspaceFolder}/StarlightDriver/StarlightDriver.csproj"
        }
    ]
}